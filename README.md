# Fictional Character Personality Quiz

A quiz app that matches you with a fictional character based on your personality traits. Uses graph embeddings (Node2Vec) trained on character-trait relationships.

## Installation

```bash
uv sync
```

## Usage

```bash
uv run python -m quiz_app
```
